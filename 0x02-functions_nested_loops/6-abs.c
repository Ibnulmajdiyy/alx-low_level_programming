#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: 'the program's description'
 *
 * @n: int in question
 *
 * Return: abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
